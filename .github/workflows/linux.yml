name: ubunturdp
on: 
  workflow_dispatch:
    inputs:
      auth:
        description: 'GRDP Authorization Code'
        required: true
        default: 'paste your code here'
   
defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Creating User to Login
      run: |
         # Create user 'Ubuntu' with password '1234'
         sudo useradd -m Ubuntu 
         sudo adduser Ubuntu sudo
         echo 'Ubuntu:1234' | sudo chpasswd
         sudo sed -i 's/\/bin\/sh/\/bin\/bash/g' /etc/passwd

    - name: Installing Desktop Environment (XFCE)
      run: |
        sudo apt-get update
        sudo apt-get install -y xfce4 xfce4-terminal desktop-base xscreensaver

    - name: Installing Google Chrome Remote Desktop
      run: |
        # 1. Install dependencies explicitly first
        sudo apt-get install -y xbase-clients python3-psutil xserver-xorg-video-dummy xvfb

        # 2. Download CRD
        wget https://dl.google.com/linux/direct/chrome-remote-desktop_current_amd64.deb

        # 3. Install CRD (catch dpkg error and fix immediately)
        sudo dpkg -i chrome-remote-desktop_current_amd64.deb || sudo apt-get install -f -y

        # 4. Configure XFCE Session
        sudo bash -c 'echo "exec /etc/X11/Xsession /usr/bin/xfce4-session" > /etc/chrome-remote-desktop-session'  

        # 5. Disable Display Managers (Fixed: Added '|| true' so it doesn't crash if missing)
        sudo systemctl disable lightdm.service || true
        sudo systemctl stop lightdm.service || true
        sudo systemctl disable gdm3.service || true
        sudo systemctl stop gdm3.service || true

        # 6. Add user to CRD group
        sudo adduser Ubuntu chrome-remote-desktop

    - name: Installing Google Chrome & Tools
      run: |
        wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
        sudo dpkg -i google-chrome-stable_current_amd64.deb || sudo apt-get install -f -y
        
        sudo apt-get install -y python3-pip nautilus nano expect
        # Added --break-system-packages for newer Ubuntu versions
        pip3 install nbformat gdown pika --break-system-packages

    - name: Installing VS Code & Node
      run: |
        wget -O code.deb https://code.visualstudio.com/sha/download?build=stable\&os=linux-deb-x64
        sudo dpkg -i code.deb
        
        wget -qO- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
        source ~/.nvm/nvm.sh
        nvm install node

    - name: Generate Setup Script
      run: |
        touch setup.sh
        echo "${{ github.event.inputs.auth }}" >> "setup.sh"
        chmod 777 setup.sh

    - name: Initialize GRDP
      run: |   
        # Run the setup script as the 'Ubuntu' user
        sudo -u Ubuntu bash -c '
        expect -c "
        spawn ./setup.sh
        expect \"Enter a PIN of at least six digits: \"
        send \"123456\r\"
        expect \"Enter the same PIN again: \"
        send \"123456\r\"
        expect eof
        "
        '

    - name: Keep Alive
      run: |
        # Keeps the runner active for 6 hours max
        while true; do sleep 30; done
